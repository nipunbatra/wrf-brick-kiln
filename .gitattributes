# Git LFS tracking for large files
# Install Git LFS: git lfs install
# NetCDF files (large meteorology data)
*.nc filter=lfs diff=lfs merge=lfs -text
# Keep PNG images as regular git (small enough)
*.png !filter !diff !merge text=auto
wrf_sample/* filter=lfs diff=lfs merge=lfs -text
